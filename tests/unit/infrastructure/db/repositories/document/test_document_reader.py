# NOTE: the asset-rehydration tests that used to live in this file moved to
# test_document_graph_asset_rehydrator.py, alongside the `rehydrate_assets`
# logic itself, which now lives in its own module (graph hydration is no
# longer part of `DocumentReader`'s catalog-only responsibility).
