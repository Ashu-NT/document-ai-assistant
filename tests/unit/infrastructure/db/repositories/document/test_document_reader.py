# NOTE: the asset-rehydration tests that used to live in this file moved to
# test_document_graph_reader.py, alongside the `_rehydrate_assets` logic
# itself, which now lives on `DocumentGraphReader` (graph hydration is no
# longer part of `DocumentReader`'s catalog-only responsibility).
