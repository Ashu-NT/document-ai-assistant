# End-to-End Pipeline Audit: Raw PDF → Parsed → DB → Retrieval

Audience: the team, ahead of deciding what to harden next. Scope: the full path from a raw shipyard PDF to an engineer/technician/captain getting an answer — document graph & asset building, chunking for RAG, DB persistence, and retrieval. This audit was run as four independent deep-dives (one per subsystem), each asked to cite `file:line` evidence rather than infer from names.

Status: implementation in progress. This mirrors `doc/parsing_audit_remediation_plan.md` and `doc/pylance_diagnostics_audit.md`: findings + a phased plan, worked through phase by phase.

## Progress

- **P0.1 — done.** Token-counter/embedding-model mismatch. Added `EmbeddingSettings.max_sequence_tokens` (default 512); `ChunkingRuntimeFactory` now clamps the effective `max_chunk_tokens` against it (with a logged warning), using a conservative 1.6x word→subword expansion factor when whitespace counting is active so a word-counted budget can never map to more real tokens than the model accepts. `ChunkTokenCounterFactory` degrades gracefully to whitespace (with a warning) instead of crashing if a transformer tokenizer fails to load. Corrected `manual`/`report`/`datasheet`/`certificate` profile YAML budgets (were 1000/800/600/500 words, now 310/290/270/250 — within the safe ceiling without relying on the runtime clamp to fire under normal operation). Deliberately did **not** flip the default counter to `"transformer"`: doing so changed real chunk-type-classification behavior elsewhere (a size-sensitive threshold implicitly tuned for word-counts), indicating other thresholds likely share the same assumption — left as an opt-in (`CHUNK_TOKEN_COUNTER_PROVIDER=transformer`) rather than a silent default change, pending a dedicated pass to re-verify all size-sensitive thresholds under real-token counting.
  - **Adjacent discovery, also fixed as part of this item**: the per-document-type chunking profiles (`manual.yaml`/`datasheet.yaml`/etc.) were dead code in production — `DocumentGraphBuilder` always resolved `max_chunk_tokens`/`chunk_overlap`/`min_section_text_length` to a non-None value (its own hardcoded defaults, or `ingestion_settings.*` which was always set in `.env`), and that unconditionally beat the profile's own value via `override or policy.value`. Every document type was getting the same global budget regardless of profile. Fixed by making `IngestionSettings.max_chunk_tokens`/`chunk_overlap`/`min_section_text_length` genuinely optional (`int | None`, default `None`), removing `DocumentGraphBuilder`'s hardcoded resolve-to-non-None constants, and commenting out the corresponding `.env`/`.env.example` values (previously duplicated verbatim in two adjacent blocks in both files) so they're opt-in global overrides again, not always-on. Verified: full suite 3637 passed, same single pre-existing unrelated failure, both before and after this follow-up fix.
- **P0.2 — done.** No per-element/table error isolation in `DocumentGraphBuilder.build`. Wrapped the per-element materialization loop (`document_graph_builder.py`) in a per-element try/except mirroring the established `docling_document_normalizer.py` pattern: a failing element (e.g. malformed table metadata) is skipped and recorded rather than sinking the whole document, with any tentatively-added table/picture asset rolled back so the graph never ends up with an orphaned asset pointing at an element that was never added. Raises `ChunkingError` only if every element failed (mirroring the normalizer's "raise only if zero elements survive" rule). Threaded the new `skipped_element_errors` list through `parsing_workflow.py` into `build_parsing_workflow_result`'s `parse_warnings`, exactly like the existing `normalization_item_errors` path, so partial failures are visible rather than silent. Added two regression tests (`_test_document_graph_builder_part13.py`): one bad element doesn't sink good elements in the same document, and all-elements-fail still raises. Verified: full suite 3639 passed (2 new tests), same single pre-existing unrelated failure.
- **P0.3 — done.** `cross_reference_expansion_enabled` defaulted to `False` in code (`retrieval_settings.py`), only working today because `.env` overrode it — a silent regression risk if that var were ever reset. Flipped the code default to `True` (matching the only real consumer, `retrieval_runtime_builder.py`) and documented it in `.env.example` (previously undocumented there entirely). Verified: full suite 3639 passed, same single pre-existing unrelated failure.
- **P0.4 & P0.5 — done (scoped).** No FK enforcement anywhere; `ingestion_runs.document_id` not a real FK. Added a SQLAlchemy `connect` event listener in `session.py` that issues `PRAGMA foreign_keys=ON` for SQLite (a no-op on Postgres/MySQL, which enforce FKs by default) — every FK declared on the ORM models is now actually enforced at the DB layer, so an incomplete delete path fails loudly with an `IntegrityError` instead of silently leaving orphaned rows. Fixed `IngestionRunORM.document_id` to be a real `ForeignKey("documents.id", ondelete="SET NULL")` (SET NULL, not CASCADE — an ingestion run is audit history that should outlive the document it refers to). Added Alembic migration `a3f7c8e2d451` using `batch_alter_table` (SQLite can't `ALTER` in a FK constraint on an existing column without recreating the table; verified upgrade and downgrade both apply cleanly against a scratch DB from an empty schema, and confirmed via `PRAGMA foreign_key_list` that the constraint lands correctly).
  - **Deliberately scoped down**: did not add `ondelete=` policies to the other ~24 FKs across the ORM layer (sections/elements/chunks/extraction tables/vector mappings, etc.) to match `DocumentWriter`'s existing manual cascade-delete behavior. That's a much larger, higher-risk change — SQLite requires a batch-mode table recreation per table, and each FK needs its own considered semantic (CASCADE vs. SET NULL vs. RESTRICT), not a blanket policy. The `PRAGMA foreign_keys=ON` fix already delivers the core safety property (integrity is enforced, failures surface immediately) without that risk. Flagged as a good candidate for a dedicated follow-up. Full test suite passed unchanged with FK enforcement now active, confirming the app's existing insert/delete ordering was already FK-consistent in practice. Verified: full suite 3639 passed, same single pre-existing unrelated failure.

All five P0 items from this audit are now done.

- **P1.6 — done.** Unbounded procedure/list-run overflow and non-list content splitting mid-sentence. Two changes: (1) `chunk_fragment_packer.py`'s `_should_flush_before_list_run` no longer bails out when a list run is itself larger than a whole chunk — it now always flushes before a new run starts if adding the whole run would overflow, whether or not the run fits standalone. Splitting across multiple chunks is still unavoidable for an oversized run, but the run's own first chunk now starts clean instead of glued to unrelated preceding content (updated the one existing test that encoded the old behavior). (2) `chunk_text_splitter.py` gained a `_split_clauses` level between sentence-splitting and the blind raw-token-window fallback — only matters when a single sentence alone exceeds `max_chunk_tokens` (long compound safety-warning sentences with conditional clauses are the realistic case), splitting on semicolons or before a comma-joined conjunction (and/or/unless/if/since/because/etc.) so a forced split lands on a clause boundary instead of mid-clause. Added regression tests for both. Verified: full suite 3640 passed (net +1 test), same single pre-existing unrelated failure.
- **Urgent fix discovered mid-P1, not on the original list**: enabling `PRAGMA foreign_keys=ON` in P0.4 exposed a real bug in `DeleteDocumentWorkflow.run()` — it deleted `chunks`/`documents` rows while `chunk_vectors` rows still referenced them via FK (`document_id`/`chunk_id`, no `ondelete=`), which used to silently succeed and is now correctly rejected by SQLite with an `IntegrityError`. Verified this empirically against a real scratch SQLite DB before and after the fix. Root cause: `chunk_vectors` cleanup was deliberately deferred until after the SQL delete commits (so a later Qdrant failure never leaves a document half-deleted in SQL — a real, worth-keeping design intent, not the bug itself). Fixed by splitting vector cleanup into two phases: `unit_of_work.vector_mappings.list_qdrant_point_ids_by_document(document_id)` is called first (read-only, before anything is deleted) to capture the point IDs, then `unit_of_work.vector_mappings.delete_document_mappings(document_id)` runs inside the *same* SQL transaction as everything else (before `chunks`/`documents`, satisfying FK ordering), and finally a new `VectorStore.delete_vector_points(point_ids)` method (extracted from the existing `delete_document_vectors`, added to the `VectorStore` protocol and its `QdrantVectorStore`/`EmbeddingWorkflow` implementations) deletes the actual Qdrant points after the SQL transaction commits — preserving the original failure-mode intent while fixing the FK ordering. Updated the unit test's fakes and call-order assertions to match (`unit_of_work.commit_count` dropped from 2 to 1, since Qdrant cleanup no longer needs a second SQL commit). Verified: full suite 3640 passed, same single pre-existing unrelated failure.
- **P1.8 — done.** Corpus-specific hardcoded heuristics (`_BRANDING_HEADERS` in `section_header_filter.py`, `_UMBRELLA_WORDS` in `layout_heuristic_strategy.py`). Extracted both into a new `src/config/section_hierarchy/corpus_heuristics.yaml`, loaded via a small cached loader module (`corpus_heuristics_config.py`, mirroring the existing `chunking_policy_loader.py` pattern: strict `load_yaml_config`, no silent fallback). Both classes gained a constructor param (`branding_headers`/`umbrella_words`, both `frozenset[str] | None = None`) that defaults to the loaded config but can be injected directly — production behavior is unchanged (same values, same defaults), but onboarding a new shipyard's documents with different boilerplate/vocabulary is now a config edit, not a code change. Added test coverage that didn't exist before for `SectionHeaderFilter` at all, plus new tests for the config loader and the constructor-override path on both classes. Verified: full suite 3647 passed (+7 new tests), same single pre-existing unrelated failure.
- **P1.9 — done (scoped).** Formulas/code/forms get no structured asset representation, no `FOOTNOTE` type. Scoped this to what's genuinely fixable without inventing a new Asset-entity subsystem (that would mean new domain entities, ORM tables, migrations, mappers, and builder logic on the scale of `TableAsset`/`PictureAsset` — a much larger, separate initiative with real design ambiguity about what fields matter per type, not something to improvise silently). What's fixed: (1) added `ElementType.FOOTNOTE` and wired it in `docling_item_extractor.py` — Docling's own `footnote` label was already being silently mapped to generic `TEXT` with no branch to catch it. (2) Added `ChunkType.FORMULA`/`ChunkType.CODE_BLOCK`; `chunk_fragment_builder.py` now tags `ElementType.FORMULA`/`ElementType.CODE` elements with these deterministic types and `standalone=True`, giving them the same isolation treatment tables/pictures already get (never merged into unrelated surrounding prose, never subject to arbitrary token-budget mid-splitting) instead of flattening into generic `GENERAL` text. Added both new types to `ChunkTypeResolver._SPECIAL_CHUNK_TYPES` (preserved through classification, not re-scored) and to `ExtractionCandidateSelector._CHUNK_TYPE_CANDIDATES` (mapped to `frozenset()`, matching the existing `CERTIFICATION_INFO`/`DRAWING_REFERENCE` "identifiers only" precedent — a completeness test caught this immediately when it was missing). Added test coverage for all of it, including new coverage for `DoclingItemExtractor.extract_element_type` which had none before. Still not fixed, flagged as a larger follow-up: no per-row/structured-field capture for FORM elements, no dedicated Formula/Code asset entities. Verified: full suite 3653 passed (+6 new tests), same single pre-existing unrelated failure.
- **P1.10 — done (already substantially covered).** Delete-cascade completeness is workflow-enforced, not DB-enforced; risk if a future code path deletes documents directly. Verified there is exactly one code path anywhere in the codebase (including `scripts/`) that deletes a document — `DeleteDocumentWorkflow` — everything else (`scripts/cleanup_duplicate_documents.py` included) routes through it. That one path is now correct (fixed mid-P1, see above). The P0.4 `PRAGMA foreign_keys=ON` fix already changes the failure mode this finding is about: a hypothetical future direct-delete path that skips the proper cascade order no longer silently orphans rows — it now fails loudly with an `IntegrityError`, which is the practical protection this finding was asking for. What remains — DB-level `ondelete=CASCADE` across the other ~24 FKs so a direct delete could succeed automatically instead of just failing safely — is the same large, deliberately-deferred batch-migration initiative already scoped out in P0.4/P0.5 (per-FK cascade-vs-set-null-vs-restrict judgment, SQLite table recreation per table); not re-attempting it a second time under a different item number without new information that would change that call.

All ten items from this audit (P0 and P1) are now done.

## P2 progress

Scope decision before starting: items #12 (drawing-ID cross-reference patterns), #14 (maritime jargon synonym expansion), and #15 (reranker weight validation against a relevance-judged eval set) all require real corpus evidence, domain vocabulary, or human relevance judgments that don't exist yet — inventing plausible-looking patterns/synonyms/weights without validating them against real data is exactly the kind of unvalidated guess the original audit warned against. Skipped, left documented as blocked on data rather than attempted. Items #11, #13, #16, #17, #18 are genuinely code-only and were implemented.

- **P2.11 — investigated, partially corrects the original finding, remainder deferred.** "No per-row/per-cell bbox traceability inside tables." Traced the full path from Docling extraction to persistence: `DoclingTableCellCandidateBuilder.build()` already calls `DoclingProvenanceExtractor.extract_bbox(cell)` **per cell**, populating `TableCellSpan.bbox` for every individual cell (`docling_table_cell_candidate_builder.py:55`) — this is genuinely captured, not missing. `TableCellSpan.to_dict()`/`from_dict()` correctly round-trip `bbox` through the `elements.parser_extra_json` blob (there's no dedicated `tables` DB table — `TableAsset` is entirely rehydrated from element metadata on read, confirmed via `document_graph_asset_rehydrator.py`), so per-cell bbox survives persistence intact. The original audit's "only one bounding box per whole table" characterization was based on `SourceLocationFactory`/the element's own `SourceLocation` (correct: that IS one bbox per element) without accounting for the separate, already-present `TableCellSpan.bbox` on each cell. **What's actually still missing**: this per-cell bbox data has zero consumers outside internal table-structure reconstruction (`tables/rows/`, `tables/structure/`, `tables/normalization/`) — `table_evidence_hydrator.py` (the actual QA/citation-facing consumer) never reads it, and `Citation`/`RetrievedChunk` only carry one `SourceLocation.bbox` per chunk, not per-row. Wiring per-cell bbox into citations needs a real product/UX decision (what does a "per-row citation" look like — a list of row bboxes on `Citation`? A separate row-citation type? Only surfaced for spare-parts/maintenance tables specifically?) that isn't answerable from the code alone, same category of decision as the P1.9 Formula/Code-asset scoping call. Not implemented further; documented here so the next person doesn't re-discover that the capture layer already works and waste time re-building it.
- **P2.16 — done.** Fixed top-k regardless of query intent/breadth. `QuestionAnsweringRouter.decide()` previously received `top_k=request.top_k or 5` — a hardcoded literal (not even `retrieval_settings.final_retrieval_top_k`) applied to every question regardless of intent, resolved *before* intent classification even ran. Added `src/config/retrieval_intent/intent_top_k.yaml` (overrides for `identifier`/`specification` down, `troubleshooting`/`procedure`/`maintenance` up — the only intents with a structural reason to need fewer or more chunks, not tuned weights requiring eval data) loaded via a small cached loader (`retrieval_query_intent_top_k_config.py`, same pattern as `corpus_heuristics_config.py`). `QuestionAnsweringRouter.decide()`'s `top_k` param is now `int | None`; when the caller doesn't pass one explicitly, the router resolves intent first and then sizes `top_k` from the override table (falling back to `retrieval_settings.final_retrieval_top_k` for any unlisted intent) — an explicit caller-supplied `top_k` always wins. Fixed the call site (`question_answering_workflow.py`) to stop collapsing `None` to a hardcoded `5`. Added tests for the config loader and for the router's sizing logic using a fake query analyzer with a pinned intent (avoids depending on fuzzy natural-language intent classification for a deterministic test). Verified: full suite 3663 passed (+7 new tests), same single pre-existing unrelated failure.

- **P2.18 — done.** Composite `(document_id, sequence_number)` index on `chunks`. Replaced the single-column `ix_chunks_document_id` index with a composite `ix_chunks_document_id_sequence_number` in `ChunkORM.__table_args__` — the composite index already covers document_id-only lookups via the leftmost-prefix rule, so keeping both would have been pure redundant write overhead. Added Alembic migration `b7d4e1f92c68` (`batch_alter_table` not needed here since SQLite supports plain `CREATE`/`DROP INDEX`); verified upgrade and downgrade both apply cleanly against a scratch DB, confirmed via `PRAGMA index_list`/`PRAGMA index_info`. Verified: full suite 3653 passed, same single pre-existing unrelated failure.
- **P2.17 — done.** Table/figure cross-reference resolution was caption-numbering-format-dependent with no fallback. `ChunkAssetNumberIndex` now also tracks every table/figure-bearing chunk's page regardless of caption (`nearest_table_chunk`/`nearest_figure_chunk`, bounded to a 1-page window matching `LogicalTableFamilyResolver`'s existing `max_continuation_page_gap` precedent). `ChunkAssetReferenceResolver.resolve_table`/`resolve_figure` gained an optional `source_page` param; when caption-based matching finds zero candidates, it now falls back to the nearest table/figure on an adjacent page instead of going straight to `UNRESOLVED`, tagged `RESOLVED_AMBIGUOUS` at a new, deliberately low `0.3` confidence (below the existing `0.5`/`0.75` tiers, since this is a proximity guess, not an actual label match). `ChunkCrossReferenceLinker` now threads the source chunk's page through to both resolver calls. Backward compatible: the fallback only activates when `source_page` is explicitly passed (existing direct resolver-level tests that don't pass it are unaffected). Added regression tests for the fallback triggering, not triggering without a source page, and not triggering beyond the page window, plus a linker-level integration test. Verified: full suite 3667 passed (+4 new tests), same single pre-existing unrelated failure.

## P2 summary

Items #11, #13, #16, #17, #18 implemented and verified. Items #12, #14, #15 documented as blocked on real corpus evidence/domain vocabulary/human relevance judgments — deliberately not attempted with fabricated data. All P0, P1, and actionable P2 items from this audit are now done.

- **Deferred item from P0.4/P0.5 and P1.10 — done.** DB-level `ondelete=` CASCADE/SET NULL migration across the remaining FKs (scoped out twice before as "~24 FKs, higher-risk"; true count turned out to be 54 across ~20 tables). Every FK's policy was decided from actual application deletion code, not guessed: `document_id` is CASCADE everywhere (the real ownership boundary — e.g. `ExtractionWriter._delete_extraction_result` always deletes by `document_id`, never by `extraction_id`), `extraction_id`/`source_chunk_id`/`equipment_id`/`maintenance_task_id`/`section_id`/`parent_section_id` are SET NULL (lineage/reference tags whose staleness shouldn't destroy the owning row), `target_chunk_id` on cross-references is SET NULL while `source_chunk_id` is CASCADE. The highest-leverage single edit was `ExtractionEntityColumnsMixin` (`@declared_attr` columns, covers 33 FKs across all 11 extraction-entity tables at once); the rest were per-file edits to `document_models.py`, `classification_models.py`, `vector_models.py`, and five standalone extraction ORM files. Four new Alembic migrations (`c1d9e3f5a827` → `d2e4f6a8b913` → `e5f7a9c1d024` → `f6a8c2e4b135`) use `batch_alter_table(recreate='always')` with an explicit `naming_convention` (needed because `Base` has none configured, so every pre-existing FK is anonymous and otherwise untargetable by `drop_constraint`) — each verified independently both directions (upgrade/downgrade/upgrade again) against a scratch SQLite DB via `PRAGMA foreign_key_list`.
  - **Adjacent discovery, also fixed**: `contact_points` had zero migration history anywhere in the repo (confirmed via `grep -rl "create_table('contact_points'"` across every migration returning nothing, while all 10 sibling extraction-entity tables had one) — a genuine pre-existing gap, unrelated to this task but caught while altering that table's FKs. Added the missing `create_table` (with correct `ondelete=` from inception) to `e5f7a9c1d024`.
  - **Severe pre-existing production bug found and fixed, not caused by this work**: the integration test engine (`tests/integration/db/conftest.py`) never actually enabled `PRAGMA foreign_keys=ON`, unlike the production engine (`session.py`) — meaning the P0.4 claim that "full suite passed with FK enforcement active" was never actually true for any integration test. Fixing that test-engine gap (a natural part of verifying this task) immediately exposed a live, in-production bug: SQLAlchemy's automatic flush-time INSERT ordering does not topologically sort across ORM classes that lack an explicit `relationship()` between them, even with fully correct `ForeignKey` column metadata — so `DocumentWriter`/`ExtractionWriter` were silently attempting to insert `chunks` before their parent `documents`/`sections` existed (and extraction entities before their parent extraction result). Confirmed via `git stash` that this reproduces identically on unmodified pre-session code, i.e. it has been live since `PRAGMA foreign_keys=ON` was first turned on for SQLite. Fixed by adding explicit `session.flush()` calls between insert stages in both writers' `_merge_document_structure`/`_merge_chunk_artifacts` and `_insert_extraction_result`.
  - **Test-fixture debt surfaced by the same test-engine fix, also fixed**: ~40 integration tests across `test_classification_repository.py`, `test_extraction_repository.py`, `test_identifier_repository.py` (`_part1`/`_part2`), and `test_vector_mapping_repository.py` were passing only because FK enforcement was silently off. Two distinct fixture bugs: (1) `_test_identifier_repository_part1.py`/`_part2.py`'s local `_make_chunk()` helper (and one inline `DocumentChunk` construction) defaulted to a `section_id` that no test ever actually created — fixed by defaulting to `None` (nullable, and no test in either file depends on section data). (2) `sample_extraction_result`/`sample_document_classification`/vector-mapping tests all reference a shared `document_id="doc_001"`/`chunk_id="chunk_001"` via fixtures in `tests/fixtures/extraction.py`/`document_graph.py`, but no test file actually seeded those rows first. Added an opt-in (not autouse — `test_unit_of_work.py`'s rollback tests specifically assert nothing persists, so a global autouse seed would have broken them) `seed_document_with_chunks` factory fixture in `tests/integration/db/conftest.py`, wired via `pytestmark = pytest.mark.usefixtures(...)` in the three affected test modules.
  - Verified: full suite 3667 passed, same single pre-existing unrelated failure (`test_parsing_workflow_hybrid_ocr.py::test_parse_runs_optional_page_ocr_fallback_before_graph_build`) — identical pass count to before this task started, confirming zero regressions despite ~40 tests changing from "silently not testing FK enforcement" to "actually testing it and passing." `npx pyright` clean (0 errors) on every touched file.
- **P1.7 — done.** Hard deletes had no before-state audit capture (`audit_tracker.py:28-37`'s `record_success` never passed `before_state`, even though `audit_service.record`, the `AuditRecord` domain type, and the `audit_records.before_state_json` column already supported it end-to-end — the gap was purely that nothing ever populated it). Added `ActionResult.before_state: dict | None = None` and wired `AuditTracker.record_success` to forward it. Scoped the actual snapshot to what's genuinely free: `DeleteDocumentWorkflow.run` already calls `unit_of_work.documents.get_document_entry(document_id)` before deleting anything (to check existence) — that `DocumentCatalogEntry` (title, file name/path, type, page/chunk/section/identifier/table/picture counts, created_at) is now returned as `before_state` on the `ActionResult` the workflow returns, instead of `run` returning `None`. Deliberately did not snapshot the full deleted row set (every chunk/extraction-entity/vector-mapping row) — that would need extra reads per delete and is closer to a soft-delete/backup subsystem, a separate initiative from "make the audit log show what was deleted" which this fixes. No DB migration needed (column already existed, unused). Verified: full suite 3668 passed (net +1 test), same single pre-existing unrelated failure. `npx pyright` clean (0 errors) on every touched file (pre-existing, unrelated `Protocol` strictness errors in `test_delete_document_workflow.py`'s test fakes confirmed via `git stash` to predate this change).
- **P2.13 — done.** `GENERAL` chunks got minimal embedding enrichment relative to other chunk types. `chunk_embedding_enricher.py`'s `enrich_embedding_text` previously short-circuited entirely (`return base_text`) for any chunk type outside a curated `ENRICHED_CHUNK_TYPES` set unless table metadata was present — meaning `GENERAL` chunks (the plurality type) got only the title/section-path prefix `ChunkPayloadFactory` already adds unconditionally, nothing from the enricher itself. Removed that gate for the two enrichments that are generically safe for any chunk type: Section/Component framing lines, and the generic (non-chunk-type-specific) related-term matching that `build_chunk_related_terms` already runs unconditionally internally. Chunk-type-*specific* alias branches (e.g. "hazard"/"safety warning" for `SAFETY_WARNING`) stay correctly gated to their own types — verified with a dedicated test that they don't leak onto unrelated `GENERAL` chunks. Updated three existing tests that had encoded the old "no enrichment for GENERAL" behavior as their expected outcome (two in `test_embedding_service.py`, one in `_test_chunk_payload_factory_part1.py`) — in two cases the fix was isolating what was actually under test (base-text selection logic) from the now-broader enrichment behavior; in the third, updating the assertion to the new correct behavior. Added 3 new regression tests. Verified: full suite 3656 passed (+3 net new tests), same single pre-existing unrelated failure.

## Direct answers to the questions asked

**Does it meet enterprise standard?** Mostly, with specific named gaps. Migration hygiene is genuinely strong (single clean head, verified by hand). Query efficiency is strong (no N+1 patterns found anywhere sampled). Referential integrity was the one weak area — *(update: now fixed; see the "Deferred item from P0.4/P0.5 and P1.10" entry in the P2 summary — `PRAGMA foreign_keys=ON` plus `ondelete=` CASCADE/SET NULL policies now cover all 54 FKs, DB-enforced not just application-enforced)*. Deletes are pure hard-deletes with no before-state audit capture. One audit-trail table (`ingestion_runs`) isn't even a real foreign-key child of `documents` — *(also fixed, see P0.4/P0.5)*.

**Good for RAG?** Yes in structure, but there's one finding that undercuts everything else: chunk token budgets are measured in whitespace-split words, not real embedding-model tokens, while the default embedding model (`BAAI/bge-small-en-v1.5`) has a hard 512-token limit and the `manual` profile allows 1000 "tokens" (words). For technical prose full of part numbers and unit codes, that gap is large enough that the tail of large manual chunks is likely **silently truncated by the embedding model** — stored in the DB, never actually retrievable. This is the single highest-impact finding in the whole audit.

**Is all the linking excellent?** Detection is excellent and self-documented (the code explicitly notes which reference patterns were included/excluded and why, e.g. drawing-ID patterns deliberately excluded for lack of corpus evidence). Resolution is adequate, not excellent: section/chapter references resolve via fuzzy title-matching, not the same precision as page references, and cross-reference expansion **defaults to `False` in code** — it only works today because `.env` overrides it, which is a latent regression risk if that env var is ever dropped.

**Is asset building good?** Adequate. Tables and pictures get rich, well-engineered treatment (multi-page table family resolution, parallel-stream/multi-column detection, per-shipyard-table-type normalizers). But formulas, code blocks, and forms get no structured representation at all — they flatten to generic text — and there's no per-row/per-cell bounding box, only whole-table location, so you can trace an answer to a table but not to the specific row inside a dense spare-parts list.

**Can it handle most technical documents?** For the patterns already seen in this corpus, yes — multi-page tables, multi-column layouts, TOC-based fallback, and several shipyard-specific table families (spare parts, maintenance schedules, troubleshooting, performance curves) all have dedicated, well-commented handling. But some of that handling is narrowly reverse-engineered from this corpus specifically (hardcoded branding-header strings, a hardcoded English "umbrella word" list for heading nesting) and will need revisiting per new document source rather than generalizing automatically.

**Easy retrieval for regular engineer/technician/captain questions?** Strong for the queries that matter most in this domain — part numbers, identifiers, spec lookups all have a dedicated fast path with real hybrid fusion (RRF across keyword/vector/structured) and a large reranker bonus for identifier matches. Table-based answers (torque specs, schedules) get real structured reconstruction, not a flattened blob. Weaker for open conceptual questions: no synonym/jargon expansion beyond identifier-label abbreviations, and the reranker is hand-weighted heuristics never validated against a relevance-judged eval set. Final top-k is fixed at 5 regardless of whether the question is narrow ("part number for X") or broad ("how do I troubleshoot alarm code Z").

---

## Subsystem 1: Document Graph & Asset Building

Scope: `builders/document_graph/`, `builders/section_hierarchy/`, `parsing/tables/`.

| # | Question | Verdict |
|---|---|---|
| 1 | Asset building completeness | Adequate |
| 2 | Linking quality | Strong detection / adequate resolution |
| 3 | Section hierarchy robustness | Adequate |
| 4 | Technical-document handling | Adequate |
| 5 | Enterprise standard (traceability/isolation) | Weak on isolation, adequate on traceability |

Key findings:
- `document_graph_builder.py:226-240` — only `TABLE`/`PICTURE` element types get structured assets. `FORMULA`/`CODE`/`FORM` fall through to plain text; no `FOOTNOTE` type exists at all.
- `document_graph_builder.py:160-340` — **the entire graph build (sections, all tables/pictures, family resolution, chunking, cross-ref linking) is one try/except.** A single malformed table or one exception in `TableSemanticResolver`/`LogicalTableFamilyResolver` fails the *whole document*, discarding everything correctly built so far. This is the most impactful finding in this subsystem — it's the one gap the earlier per-item normalization isolation work (Phase 1, `docling_document_normalizer.py`) didn't reach, because that fix was upstream of graph building.
- `chunk_cross_reference_detector.py:25-34` — section/chapter references resolve only via fuzzy chunk-title matching, lower precision than page-based refs.
- `chunk_cross_reference_detector.py:46-51` — drawing-ID patterns ("Drawing SK-1044") deliberately excluded, explicitly documented as unverified against this corpus. Worth flagging to stakeholders since drawing references are common in maritime manuals.
- `parsed_asset_factory.py` / `source_location_factory.py` — only one bounding box per whole table/picture; row IDs are synthetic (`f"{table_id}:row:{index}"`), no per-row bbox.
- `section_builder.py:197-198` — silently `continue`s (drops the element) if `active_section` is ever `None`; narrow edge case, no logging, so a future regression here would silently vanish elements with no audit trail.
- `section_header_filter.py:5-10` (`_BRANDING_HEADERS`) and `layout_heuristic_strategy.py:12-20` (`_UMBRELLA_WORDS`) — hardcoded, corpus-specific string/word lists driving structural decisions. Will misfire on manuals from other vendors/shipyards.
- Positive: `logical_table_family_resolver.py:83-207` (multi-page + multi-column table continuation) and the per-table-family normalizers (`spare_parts_table_normalizer.py`, `maintenance_schedule_table_normalizer.py`, etc.) are thorough and well-commented, including documented historical bug fixes. `asset_nearby_text_enricher.py` + `asset_metadata_synchronizer.py` correctly propagate caption/OCR/nearby-text onto chunk metadata for retrieval.

## Subsystem 2: Chunking & RAG-Readiness

Scope: `builders/chunking/` (all subfolders).

| # | Question | Verdict |
|---|---|---|
| 1 | Chunk boundary quality | Adequate |
| 2 | Self-sufficiency for retrieval | Strong |
| 3 | Structured content handling | Strong |
| 4 | Token budget / sizing | **Weak** |
| 5 | Deduplication & noise | Adequate |
| 6 | Semantic signal tagging | Strong |

Key findings:
- `chunking_settings.py:7-9` — default token counter is `WhitespaceChunkTokenCounter` (word count), not the real embedding-model tokenizer, even though a transformer-based counter exists and is wired to the embedding model elsewhere (`chunk_token_counter_factory.py:24-34`).
- `.env.example:33` + `src/config/chunking/manual.yaml:3` — default embedding model `bge-small-en-v1.5` has a 512-token hard limit; the `manual` profile allows 1000 word-tokens. At typical technical-text word→subword expansion, this routinely exceeds 512 real tokens — **the embedding model silently truncates the tail of large manual chunks.** Stored in `content`, never retrievable. Highest-impact finding in this subsystem (and arguably the whole audit).
- `chunk_fragment_packer.py:255-257` — numbered-list-run overflow is explicitly left unhandled ("v1" per inline comment): a procedure with more steps than fit in one chunk fractures arbitrarily.
- `chunk_text_splitter.py:63-131` — non-list structured content (a long safety warning, a prose procedure) has no chunk-type-aware split protection; can be cut mid-sentence, losing a conditional clause.
- `chunk_embedding_enricher.py:19-101` — enrichment (chunk type, section, table caption/headers) only fires for `ENRICHED_CHUNK_TYPES` or table metadata; plain `GENERAL` chunks (the plurality type) get only a title/section-path prefix.
- Positive: `table_fragment_splitter.py:20-89` re-adds the header row to every split table fragment — tables never split mid-row and stay self-describing. `chunk_payload_factory.py:202-225` prefixes embedding text with document title + full section path. `chunk_type` is genuinely load-bearing downstream (60 files reference it, including intent-based retrieval preference ranking) — not vestigial metadata.

## Subsystem 3: DB Schema & Persistence

Scope: `infrastructure/db/` (orm_models, repositories, mappers, schema_management), `alembic/versions/`.

| # | Question | Verdict |
|---|---|---|
| 1 | Indexing strategy | Strong |
| 2 | Referential integrity | Strong *(fixed — see P2 summary)* |
| 3 | Nullable field discipline | Adequate |
| 4 | Migration hygiene | Strong (hand-verified) |
| 5 | Audit trail / versioning | Adequate |
| 6 | Query efficiency | Strong |

Key findings (referential integrity items below are fixed — see P0.4/P0.5 and the "Deferred item" entry in the P2 summary):
- ~~No `ondelete=` on any `ForeignKey` anywhere in the codebase, and no `PRAGMA foreign_keys=ON` listener in `session.py` — SQLite doesn't even enforce FK constraints at the DB layer. Integrity is 100% dependent on application code remembering to cascade.~~ Fixed: `PRAGMA foreign_keys=ON` (P0.4) plus `ondelete=` CASCADE/SET NULL across all 54 FKs (P2 summary deferred item).
- ~~`workflow_models.py:14` — `IngestionRunORM.document_id` is a plain indexed string, **not a real `ForeignKey`**.~~ Fixed in P0.4: real `ForeignKey("documents.id", ondelete="SET NULL")`.
- `document_writer.py:156-174` — `DocumentWriter.delete_document`'s own docstring admits extraction/vector rows aren't touched there; the full cascade is only correct when callers go through `DeleteDocumentWorkflow`. Any future direct-repository delete now fails loudly (FK enforcement) or cascades automatically at the DB layer instead of silently orphaning rows.
- ~~`audit_tracker.py:28-37` — `AuditTracker.record_success` never records `before_state`; a hard delete leaves no recoverable snapshot beyond the entity ID.~~ Fixed — see P1.7 in the P2 summary.
- Migration chain hand-traced end to end: single root → clean merge at the already-fixed `7f2a9c4e1b6d` → linear to one true head (`9c7b1e4d2a53`). Confirmed genuinely fixed, not just patched over.
- `document_graph_reader.py` (7 fixed batched queries) and `extraction_reader.py` (12 fixed queries) load full graphs/extractions without N+1; `bulk_merge.py` avoids the classic `session.merge()`-in-a-loop pattern throughout `DocumentWriter`.
- Minor: no composite `(document_id, sequence_number)` index on `chunks` for ordered retrieval — only a single-column index exists.

## Subsystem 4: Retrieval Quality

Scope: `infrastructure/retrieval/`, `application/workflows/question_answering/`, `application/workflows/retrieval/`.

| # | Question | Verdict |
|---|---|---|
| 1 | Hybrid retrieval | Strong |
| 2 | Identifier/spec-lookup fast path | Strong |
| 3 | Cross-reference-aware retrieval | Adequate (config-fragile) |
| 4 | Table/asset answer quality | Strong |
| 5 | Domain jargon / query understanding | Adequate, narrow |
| 6 | Reranking / precision | Adequate |
| 7 | Failure visibility | Strong |

Key findings:
- `hybrid_retrieval_service.py:67-170` — genuine RRF fusion across SQL-keyword, dense-vector, and structured-identifier result sets, all three enabled by default.
- `structured_evidence_resolver.py:40-69` + `retrieval_query_identifier_extractor.py:47-90` — a dedicated exact-match fast path for part/serial/model/drawing/certificate numbers, fed by the same normalization used at ingest, with a large reranker bonus (`identifier_matches * 35.0`).
- `retrieval_settings.py:89-92` — `cross_reference_expansion_enabled` **defaults to `False` in code**; it's only `True` today because `.env` sets it. If that env var is ever reset or dropped, "see Table 4"-style auto-fetch silently reverts to page-number-only answers with no error or warning.
- `table_evidence_hydrator.py:50-166` — re-fetches and reconstructs the full logical table family (handles tables split across pages/chunks), builds structured rows/headers/axis-summary plus a combined structured-text rendering — genuinely structured, not a flattened blob.
- `retrieval_query_rewriter.py:15-96` — expands ~20 identifier-label abbreviations (p/n, dwg no., s/n, etc.) but has no semantic synonym layer for conceptual maritime jargon (generator/genset, valve/cock). Dense retrieval falls back to plain cosine similarity for anything not identifier-shaped.
- `deterministic_hybrid_reranker.py:36-127` — fully rule-based, hand-weighted scoring (e.g. `chunk.score*8.0`, `identifier_matches*35.0`); never validated against a relevance-judged eval set. `top_k` fixed at 5 final regardless of query breadth.
- `retrieval_evidence_guardrail.py:16-62` — explicit `NO_EVIDENCE`/`INSUFFICIENT_EVIDENCE` decisions with safe user-facing messages rather than letting generation proceed on weak evidence. This is a real strength for a domain where a wrong answer (wrong torque spec, wrong safety precaution) has physical consequences.

---

## Consolidated Prioritized Remediation Plan

**P0 — highest impact, recommend addressing first:**
1. **Token-counter/embedding-model mismatch** (`chunking_settings.py`, `chunk_token_counter_factory.py`, `manual.yaml`) — default to the transformer-based counter, or hard-cap `max_chunk_tokens` against the actual embedding model's limit. Silent, undetectable data loss otherwise.
2. **No per-element/table error isolation in `DocumentGraphBuilder.build`** (`document_graph_builder.py:160-340`) — one bad table currently fails an entire document. Needs the same per-item isolation pattern already applied upstream in `docling_document_normalizer.py`.
3. **`cross_reference_expansion_enabled` defaults to `False` in code** (`retrieval_settings.py:89-92`) — flip the code default to match the intended behavior, don't rely on `.env` alone for a feature this central to answer quality.
4. **No FK enforcement anywhere** (`session.py`, all `orm_models/*.py`) — add `PRAGMA foreign_keys=ON` for SQLite and `ondelete=` policies on foreign keys.
5. **`ingestion_runs.document_id` isn't a real ForeignKey** (`workflow_models.py:14`) — fix so the audit-trail table itself can't silently orphan.

**P1 — real gaps, address next:**
6. Unbounded procedure/list-run overflow (`chunk_fragment_packer.py:255-257`) and non-list structured content splitting mid-sentence (`chunk_text_splitter.py`).
7. ~~Hard delete with no before-state audit capture (`audit_tracker.py:28-37`) — consider soft-delete or a pre-delete snapshot.~~ Done — see P1.7 in the P2 summary (audit log now captures a document-metadata snapshot; full soft-delete/row-level snapshot deliberately out of scope).
8. Corpus-specific hardcoded heuristics (`_BRANDING_HEADERS`, `_UMBRELLA_WORDS`) — extract to config/corpus-specific override rather than literal code, ahead of onboarding a new shipyard's documents.
9. Formulas/code/forms get no structured asset representation, no `FOOTNOTE` type.
10. Delete-cascade completeness is workflow-enforced, not DB-enforced (`document_writer.py:161-163`) — risk if a future code path deletes documents directly.

**P2 — worth doing, lower urgency:**
11. No per-row/per-cell bbox traceability inside tables.
12. Section/drawing-ID cross-references detected-only or excluded — extend once corpus evidence supports it.
13. `GENERAL` chunks get minimal embedding enrichment relative to other chunk types.
14. No semantic/synonym expansion for maritime jargon beyond identifier-label abbreviations.
15. Reranker is fully heuristic — consider building a small relevance-judged eval set to validate/tune weights.
16. Fixed top-k regardless of query intent/breadth.
17. Table/figure cross-reference resolution is caption-numbering-format-dependent with no fallback.
18. Composite `(document_id, sequence_number)` index on `chunks`.

---

## Methodology

Four independent agent-driven deep dives, each scoped to one subsystem and instructed to cite `file:line` evidence rather than infer from file/function names, run in parallel and synthesized here without cross-editing each other's findings. Does not re-litigate items already fixed earlier this session (Docling timeout, PDF magic-byte validation, `ParserPort`, per-item normalization isolation, OCR retry, table/figure chunk-cross-reference detection, list-run continuity, stale-parser-version reingestion) — those were explicitly excluded from each agent's scope.
